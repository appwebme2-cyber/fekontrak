// Re-export useAuth from AuthContext
export { useAuth } from '@/contexts/AuthContext';

// Re-export AuthProvider from the dedicated component
export { AuthProvider } from '@/components/auth/AuthProvider';